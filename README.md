# TP Spring Boot

LARDAIS Benjamin

TPs du cours Spring Framework.

## Structure

- `src/` — code source Spring Boot
- TP01 : Hello World REST (`@RestController`, `@GetMapping`)
- TP02 : Injection de dépendances (`@Component`, `@Autowired`, `@PostConstruct`)
